int:: fibonacci(int n)
{
  int sequence::n;
  sequence::0 = 1;
  if(n == 0 || n == 1)
  {
    return sequence;
  }

  for(int i = 2, i < n,i = i + 1)
  {
     sequence::i = sequence::(i -1) + sequence::(i - 2);
  }

  return sequence;
}